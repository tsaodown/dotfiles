# Allowlist: ~/.claude/ is mostly runtime state (history, projects, sessions,
# plugins, caches, transcripts). Track only hand-edited config files.
*
!.gitignore
!CLAUDE.md
!settings.json
!statusline-command.sh
!commands/
!commands/**
