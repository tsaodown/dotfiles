Search my Obsidian vaults for context relevant to the request below, then use that context to respond.

## Vault locations

| Name | Path |
|---|---|
| `2nd brain`, `brain`, `personal` | `/mnt/c/Users/etpho/Documents/Obsidian/2nd Brain` |
| `personal notes`, `Personal` | `/mnt/c/Users/etpho/Documents/Obsidian/Personal` |

If no vault name is specified, default to `2nd Brain`.

## How to search

1. Use `grep -r --include="*.md" -l "<keywords>" <vault-path>` to find relevant files
2. For time-based queries (today, this week, recent), also check `<vault-path>/daily-notes/`
3. Read the most relevant files with the Read tool
4. Synthesize what you find into your response — quote sparingly, summarize where possible

## Request

$ARGUMENTS
